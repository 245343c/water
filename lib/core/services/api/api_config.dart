/// Set to true to use the MongoDB backend instead of in-memory mock data.
/// Switch this to true once the backend server is running.
const bool useBackend = false;

/// Local backend base URL. Change if server runs on a different port.
const String backendBaseUrl = 'http://localhost:3000/api';
